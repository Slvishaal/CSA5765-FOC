#include <stdio.h>

int main() {
    int i, n;

    printf("Enter the number of terms: ");
    scanf("%d", &n);

    printf("The first %d terms of the series are: ", n);

    for (i = 1; i <= n; i++) {
        printf("%d ", i);
    }

    return 0;
}
